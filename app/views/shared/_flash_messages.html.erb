<div class="flash-message">
  <% flash.each do |message_type, message| %>
    <div class="<%= flash_background_color(message_type) %> px-4 py-2 rounded text-white mb-4">
      <%= message %>
    </div>
  <% end %>
</div>
