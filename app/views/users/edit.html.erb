<div class="mx-5 mt-5 sm:mt-20" data-controller="video-preview">
  <h1 class="text-2xl font-bold my-20 text-center">ユーザー情報編集</h1>
    <div class="p-6 m-auto bg-slate-50 rounded-md shadow-md sm:max-w-md mt-5 sm:mt-20">
    <div class= "space-y-4">
      <%= form_with(model: @user, url: user_path(uuid: @user.uuid), local: true) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="mb-4">
          <%= f.label '名前', class: 'block text-gray-700' %>
          <%= f.text_field :name, class: 'w-full input input-bordered input-primary mt-1' %>
        </div>
        <div class="mb-4">
          <%= f.label 'メールアドレス', class: 'block text-gray-700' %>
          <%= f.email_field :email, class: 'w-full input input-bordered input-primary mt-1' %>
        </div>
        <div class="flex justify-end space-x-2 mt-4">
          <%= f.button type: 'submit', class: 'btn btn-primary' do %>
            <i class="ph ph-arrow-clockwise large-icon"></i> 更新
          <% end %>
        </div>
        <div class="mt-4 text-right">
          <ul class="menu menu-horizontal px-1">
            <li><%= link_to 'ユーザー詳細に戻る', user_path(id: @user.uuid) %></li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>
