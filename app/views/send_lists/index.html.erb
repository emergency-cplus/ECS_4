<div class="mx-5 mt-5 sm:mt-20" data-controller="video-preview">
  <h1 class="text-2xl font-bold my-20 text-center">
    送信履歴<%= render 'shared/sms_counter' %>
  </h1>
  <div class="container px-10 pb-20 mx-auto">
    <div class="overflow-x-auto">
      <%= render "send_lists/#{current_user.role}_table", send_lists: @send_lists %>
    </div>
  </div>
   <div class="flex justify-center mt-10 mb-20">
    <%= paginate @send_lists %>
  </div>
</div>
