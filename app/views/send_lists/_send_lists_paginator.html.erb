<%# The container tag
  - available local variables
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
    paginator:     the paginator that renders the pagination tags inside
-%>
<div class="join">
  <% if @send_lists.total_pages > 1 %>
    <%= link_to '1', send_lists_path(page: 1), class: "join-item btn bg-gray-700 text-white py-4" + (@send_lists.current_page == 1 ? ' btn-active' : '') %>

    <% if @send_lists.current_page > 3 %>
      <button class="join-item btn bg-gray-700 text-white py-4 btn-disabled">...</button>
    <% end %>

    <% [@send_lists.current_page - 1, @send_lists.current_page, @send_lists.current_page + 1].each do |page| %>
      <% if page > 1 && page < @send_lists.total_pages %>
        <%= link_to page, send_lists_path(page: page), class: "join-item btn bg-gray-700 text-white py-4" + (@send_lists.current_page == page ? ' btn-active' : '') %>
      <% end %>
    <% end %>

    <% if @send_lists.current_page < @send_lists.total_pages - 2 %>
      <button class="join-item btn bg-gray-700 text-white py-4 btn-disabled">...</button>
    <% end %>

    <%= link_to @send_lists.total_pages, send_lists_path(page: @send_lists.total_pages), class: "join-item btn bg-gray-700 text-white py-4" + (@send_lists.current_page == @send_lists.total_pages ? ' btn-active' : '') %>
  <% end %>
</div>
