<!-- ページの全体的なマージンとパディングを設定 -->
<div class="mx-5 mt-5 sm:mt-20" data-controller="video-preview">
  
  <!-- ページタイトルを中央寄せにし、上の余白を加える -->
  <h1 class="text-2xl font-bold my-20 text-center">アイテム詳細</h1>
  
  <div class="flex flex-col sm:flex-row justify-center items-center sm:space-x-20">

    <!-- 詳細情報 -->
    <div class="p-6 bg-slate-50 rounded-md shadow-md sm:max-w-md" style="width: 360px;">
      <div class="mb-6">
        <h1 class="font-bold">タイトル</h1>
        <p><%= @item.title %></p>
      </div>
      <div class="mb-6">
        <h1 class="font-bold">説明</h1>
        <p><%= @item.description %></p>
      </div>
      <div class="mb-6">
        <h1 class="font-bold">動画URL</h1>
        <a href="<%= @item.item_url %>"><%= @item.item_url %></a>
      </div>
      <div class='flex justify-end space-x-2 mt-4'>
        <%= link_to '編集', edit_item_path(@item), class: "btn btn-primary", id: "button-edit-#{@item.id}" %> 
        <%= link_to '削除', item_path(@item),
                    method: :delete,
                    data: { turbo_confirm: '削除しますか?', turbo_method: :delete },
                    class: "btn btn-accent", id: "button-delete-#{@item.id}" %>
      </div>
      <!-- Back link positioned at the bottom of the card -->
      <div class="flex justify-end mt-6">
        <%= link_to "一覧に戻る", items_path, class: "btn-link" %>
      </div>
    </div>

    <!-- プレビュー画面 -->
    <div class="mockup-phone" style="width: 360px;">
      <div class="camera"></div>
      <div class="display">
        <iframe
          class="absolute top-0 left-0 w-full h-full"
          src="https://www.youtube.com/embed/<%= get_youtube_shorts_id(@item.item_url) %>"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
        </iframe>
      </div>
    </div>

  </div>
</div>
